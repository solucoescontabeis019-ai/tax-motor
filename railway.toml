# Railway Configuration - Motor de Decisão Tributária

[build]
builder = "dockerfile"
buildCommand = ""

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 5
restartPolicyWindowSeconds = 60

[env]
PYTHONUNBUFFERED = "1"
